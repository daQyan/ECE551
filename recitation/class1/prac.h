#include <iostream>
#include <vector>
#include <cstdlib>
using namespace std

void sort(vector<vector<int> > v) {
  vector<int> max();
  for (int i = 0; i < v.size()-1; i++) {
    if (v[i].size() > max.size()) {
      v.push_back(v[i]);
      max(v[i]);
    }
    if (v[i].size() = max.size()) {
      for (int j = 0; j < max.size(); i++) {
        .....
        v.push_back(v[i]);
        max(v[i]);
      }
      
    }
    
  }
  
}